<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <h1>
        Groceries
    </h1>
    <ul>
        {{range .groceries}}
        <li>{{.Name}} x {{.Unit}} ({{.Price}}円) by {{.Edges.Provider.Name}}</li>
        {{end}}
    </ul>
</body>
</html>